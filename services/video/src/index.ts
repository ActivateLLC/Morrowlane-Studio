/**
 * Remotion entry point. The ShortVideo composition renders a content item's segments
 * as timed beats: heading, body line, and the visual direction as art direction notes
 * for the background layer. Register compositions here.
 *
 * This file is compiled by Remotion's bundler, not the workspace tsconfig — install
 * remotion + @remotion/cli in this directory when enabling the service.
 */
export {};
